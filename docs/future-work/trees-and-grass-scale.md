# Bigger trees and larger grass patches

**Goal:** Make trees bigger. Switch the grass texture for much bigger patches.

- Scale up tree assets/instances (or replace with larger tree models) so trees read as bigger in the world.
- Replace or rescale the grass texture so it uses much larger patches (less repetition, bigger visual “blobs” of grass).

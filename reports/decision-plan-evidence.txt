decision-plan evidence -- the same ten choices, read two ways, under one loop

Both rows are the checked-in five-character scenario (seed 1234, 160 ticks) with
Wren's ten written-down choices on her sheet, run by
tools/critic_recorded_drain_probe.gd. "Left in the source" is what the decision
function will still hand over once the run is done, so "accounted for" is what
became of the ten.

the drain probe, before the change

=== F -- the same ten choices, read two ways, under one loop
  REVIEW_EVERY=5  CONTINUE_BIAS=0.85  go_to costs 20t
  [10 of 10 turns resolved] read against what has been carried out -- 57 re-evaluations over 160 ticks
  [4 of 10 turns resolved] read as a queue -- 53 re-evaluations over 160 ticks

the same probe, after it, with what the source has left added

=== F -- the same ten choices, read two ways, under one loop
  REVIEW_EVERY=5  CONTINUE_BIAS=0.85  go_to costs 20t
  [10 of 10 resolved, 0 left in the source, 10 accounted for] read as a plan (DecisionSource.plan) -- 57 re-evaluations over 160 ticks
  [4 of 10 resolved, 0 left in the source, 4 accounted for] read as a queue (DecisionSource.recorded) -- 53 re-evaluations over 160 ticks

the same probe again at cycle 192, after a walk was spread over the ticks its span
costs (W-walk-motion). The claim is unchanged and the counts moved, because a
character that walks while it walks meets things sooner: the five-character run's
quarrel now starts at tick 59 rather than 77, so there are four fewer mid-action
re-evaluations on the plan reading and four fewer on the queue reading.

=== F -- the same ten choices, read two ways, under one loop
  REVIEW_EVERY=5  CONTINUE_BIAS=0.85  go_to costs 20t
  [10 of 10 resolved, 0 left in the source, 10 accounted for] read as a plan (DecisionSource.plan) -- 53 re-evaluations over 160 ticks
  [4 of 10 resolved, 0 left in the source, 4 accounted for] read as a queue (DecisionSource.recorded) -- 49 re-evaluations over 160 ticks
